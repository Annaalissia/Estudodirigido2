9. F.U.P. que, dado um conjunto de N números, determine o menor valor, o maior valor e a soma dos valores. Faça com que ele aceite apenas números entre 0 e 1000. 

primeiro = int(input(“Qual o primeiro número da sequencia?”))
ultimo = int(input(“Qual o último número da sequencia?”))
soma = 
for c in range (primeiro, primeiro + 1, ultimo):
if ultimo > 1000:
print (“invalido”)
else:
print(“o menor numero da lista é:”, primeiro)
print(“o maior número da lista é:”, ultimo)
